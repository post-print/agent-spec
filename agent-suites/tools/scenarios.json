{
	"name": "tools",
	"description": "Native tool efficacy. Each scenario launches a real host agent.",
	"hosts": ["cursor", "claude", "openai"],
	"defaults": {
		"host": "cursor",
		"profile": "cursor",
		"skills": "none",
		"allowUserSkills": false
	},
	"scenarios": [
		{
			"name": "reads a fixture file",
			"workspace": "agent-suites/tools/workspaces/read",
			"prompt": "Read needle.txt. Reply with the exact file contents. Do not use extra tools after the read.",
			"rubric": {
				"mustReadPath": ["needle.txt"],
				"must": ["agent-test-tool-needle-7f3a"]
			}
		},
		{
			"name": "writes a marker file",
			"workspace": "agent-suites/tools/workspaces/write",
			"prompt": "Create agent-test-write-out.txt at the workspace root. The file must contain exactly one line: agent-test-write-ok-9c2e. Then stop.",
			"rubric": {
				"mustCallTool": ["Write:agent-test-write-ok-9c2e"]
			}
		}
	]
}
